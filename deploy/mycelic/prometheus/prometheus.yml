# Optional Prometheus for the compose deployment (profile "observability").
global:
  scrape_interval: 15s
scrape_configs:
  - job_name: mycelic
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /run/secrets/metrics_token
    static_configs:
      - targets: ["mycelic:8080"]
