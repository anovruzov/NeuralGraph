\documentclass[letterpaper,10pt]{article}

\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage{marvosym}
\usepackage[usenames,dvipsnames]{color}
\usepackage{verbatim}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage[english]{babel}
\usepackage{tabularx}
\usepackage{setspace}
\input{glyphtounicode}

\DeclareUnicodeCharacter{2217}{}

\setstretch{0.92}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

\addtolength{\oddsidemargin}{-0.6in}
\addtolength{\evensidemargin}{-0.6in}
\addtolength{\textwidth}{1.2in}
\addtolength{\topmargin}{-.7in}
\addtolength{\textheight}{1.4in}

\urlstyle{same}

\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

\titleformat{\section}{
  \vspace{-1pt}\scshape\raggedright\large
}{}{0em}{}[\color{black}\titlerule \vspace{-1pt}]

\pdfgentounicode=1

%-------------------------
% Custom commands
\newcommand{\resumeItem}[1]{\item\small{#1 \vspace{1pt}}}
\newcommand{\resumeSubheading}[4]{
  \vspace{-2pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{#1} & #2 \\
      \textit{\small #3} & \textit{\small #4} \\
    \end{tabular*}\vspace{-6pt}
}
\newcommand{\resumeSubItem}[1]{\resumeItem{#1}\vspace{-4pt}}
\newcommand{\resumeSubHeadingListStart}{\begin{itemize}[leftmargin=0.15in, label={}]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}
\newcommand{\resumeItemListStart}{\begin{itemize}[itemsep=2pt, topsep=2pt, parsep=0pt, leftmargin=0.2in, label={--}]}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-2pt}}

%%%%%%  RESUME STARTS HERE  %%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}

%----------HEADING----------
\begin{center}
    \textbf{\Huge \scshape Ali Novruzov} \\
    \small 713-572-7711 $|$ \href{mailto:anovruzov0708@gmail.com}{anovruzov0708@gmail.com} $|$
    \href{https://github.com/anovruzov}{github.com/anovruzov} $|$
    \href{https://linkedin.com/in/ali-novruzov}{linkedin.com/in/ali-novruzov} $|$ Austin, TX
\end{center}

%-----------PROJECT-----------
\section{Project}
\resumeSubHeadingListStart

  \resumeSubheading
    {SporeOS --- Mathematically-Grounded Brain-Inspired Memory System}{Austin, TX}
    {Creator \& Lead Architect}{December 2024 -- Present}
    \resumeItemListStart
      \resumeItem{Architected \textbf{SporeOS}, a biologically-principled episodic memory system that treats memory retrieval as a dynamical system with 9D neural signal vectors, implementing hippocampal-like temporal binding and neocortical semantic consolidation to overcome context-window limitations of current LLMs.}

      \resumeItem{Designed a novel \textbf{4D Tesseract Memory Fusion} algorithm that orthogonalizes four specialized retrieval stores (Temporal, Entity, Reasoning, Adversarial) and performs query-adaptive weighted fusion: $\text{Score}_{\text{final}} = \sum_{i=1}^{4} w_i(q) \cdot s_i$ where query type $q$ dynamically tunes weights $w_i \in [0,1]$.}

      \resumeItem{Implemented \textbf{Long-Term Potentiation (LTP)} dynamics in the neural graph: edge weights strengthened via $w_{ij}^{(t+1)} = w_{ij}^{(t)} + \alpha \cdot \text{activation}_{ij} \cdot (1 - w_{ij}^{(t)})$, modeling synaptic facilitation with saturating nonlinearity and Hebb-like learning for query-relevant memories.}

      \resumeItem{Developed a \textbf{hippocampal Temporal Store} with exponential recency decay: activation$(t) = \text{base} \cdot e^{-\lambda(T_{\text{now}} - t)}$, combined with relative date resolution (parsing ``last Tuesday'' to Unix timestamps) and temporal coherence chains enforcing $|t_i - t_{i+1}| \leq \Delta t_{\max}$.}

      \resumeItem{Engineered \textbf{neocortical semantic binding} in the Entity Store via multi-attribute factorization: each entity $E = \{a_1, \ldots, a_k\}$ embedded as $\mathbf{e} = \sum_{j=1}^{k} \gamma_j \mathbf{a}_j$ where $\gamma_j$ are learned attention weights, enabling compositional queries like ``tech workers from Austin before 2020.''}

      \resumeItem{Architected a \textbf{prefrontal cortex Reasoning Store} for breadth-first aggregation: multi-hop queries solved via graph traversal with bounded depth $d \leq d_{\max}$ and beam width $b$, computing $\text{Agg}(\text{paths}) = \frac{1}{Z} \sum_{\pi} e^{\beta \cdot \text{score}(\pi)} \text{result}(\pi)$ (softmax over reasoning paths).}

      \resumeItem{Implemented \textbf{orbitofrontal conflict detection} for adversarial reasoning: negation patterns parsed via $(X \text{ NOT } Y)$ rules, exceptions via $(X \text{ EXCEPT } Y)$, and contradictions detected by checking $P(E) + P(\neg E) > 1 + \epsilon$ across memory graph partitions.}

      \resumeItem{Developed a \textbf{22-module Neural Graph substrate} with realistic neuronal dynamics: 9-dimensional signal vectors $\mathbf{s}(t) \in \mathbb{R}^9$ per node, non-linear gating $g(\mathbf{s}) = \tanh(\mathbf{W}\mathbf{s} + \mathbf{b})$, refractory periods via $\theta_{\text{rest}} = 20\text{ms}$, and signal propagation delays $\tau_{ij} \sim \mathcal{N}(5, 2)\text{ms}$.}

      \resumeItem{Created a \textbf{4-layer hierarchical consolidation mechanism} (Message → Episode → Topic → Persona) inspired by systems consolidation theory: summaries promoted via $H(\text{summary}) < H(\text{base})$ (Shannon entropy check), merged via cross-encoder scoring, and pruned when activation $(w) < \theta_{\text{prune}}$.}

      \resumeItem{Engineered \textbf{temporal summation and cooperative amplification}: weak signals combine across synapses via $\text{activation}_i = \sigma(\sum_j w_{ij} \cdot \text{presynaptic}_j - \theta_i)$, where $\sigma$ is sigmoid and $\theta_i$ is adaptive; implements Hodgkin-Huxley-like thresholding for pattern completion and associative recall.}

      \resumeItem{Built a \textbf{Locality-Sensitive Hashing (LSH) acceleration layer} using random projections: $h_{\mathbf{r}}(\mathbf{x}) = \text{sign}(\mathbf{r} \cdot \mathbf{x})$ where $\mathbf{r} \in \mathbb{R}^{256}$, enabling $O(\log n)$ similarity lookup against 50K+ nodes without full embedding computation; combined with 117K-word synonym thesaurus for semantic robustness.}

      \resumeItem{Implemented a \textbf{hybrid retrieval scoring} with Reciprocal Rank Fusion (RRF): $\text{score}_{\text{rrf}} = \sum_k \frac{1}{\epsilon + \text{rank}_k}$, fusing BM25 (sparse), semantic embeddings, and cross-encoder reranking (dense) with configurable $\epsilon$ per query type to balance precision/recall.}

      \resumeItem{Designed a \textbf{query-type router} using linguistic pattern classification: detects 15+ markers (``when'', ``how many'', ``not'', etc.), maps to 5 retrieval modes via learned routing matrix $R \in \mathbb{R}^{5 \times 15}$, and pre-filters search space to relevant store(s), reducing retrieval latency by 60\%.}

      \resumeItem{Engineered \textbf{interference and pattern completion} mechanics: when retrieving memory $M_q$, interfering memories computed via cosine similarity; gating prevents crosstalk using orthogonal projections: $\mathbf{m}_{\text{filtered}} = (\mathbf{I} - \mathbf{P}_{\text{interference}}) \mathbf{m}_q$ where $\mathbf{P}$ projects onto interfering subspace.}

      \resumeItem{Developed \textbf{memory decay and consolidation schedules} using Ebbinghaus forgetting curve: activation decays as $a(t) = a_0 \cdot e^{-t/\tau}$ where $\tau = f(\text{retrieval\_count}, \text{importance})$, with threshold-based archival: warm memories → cold summaries when $a(t) < a_{\text{threshold}}$.}

      \resumeItem{Built a \textbf{wavefront signal propagation model} for spreading activation: each query launches waves through the graph with velocity $v = 0.5$ (nodes/ms), attenuation $\alpha = 0.95^d$ per hop, and resonance at nodes with prior coactivation history, implementing biologically-plausible parallel search.}

      \resumeItem{Achieved \textbf{65\% accuracy} on LoCoMo benchmark via 4D fusion (vs. 38\% single-store baseline): +30\% on temporal reasoning (Ebbinghaus decay + temporal chains), +10\% adversarial (conflict detection), +8\% multi-hop (reasoning store aggregation).}

      \resumeItem{Engineered full async architecture (asyncio, asyncpg, aiosqlite) enabling $>1000$ concurrent memory queries with $<50$ms P95 latency, implementing lock-free MVCC (multi-version concurrency control) for graph reads and write-once consolidation logs.}
    \resumeItemListEnd

\resumeSubHeadingListEnd

\end{document}
