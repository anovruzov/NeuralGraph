# Large per-run traces (routing paths, per-record dumps) are regenerated by the experiments; manifests,
# metrics.json and the processed CSVs are committed so every reported number is traceable.
results/raw/**/*.jsonl
results/raw/**/*.jsonl.gz
results/logs/
__pycache__/
*.pyc
.pytest_cache/
