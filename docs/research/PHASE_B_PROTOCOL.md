# Phase B protocol (LLM experiments) — every agent follows this
1. Worktree is at git HEAD: FIRST copy today's uncommitted files from /Users/nurmanmahammadov/Desktop/NeuralGraph into your worktree at the same relative paths:
   demo/runner.py demo/retrieval_eval.py NeuralGraph/llm_backend.py NeuralGraph/answering.py NeuralGraph/reranker.py NeuralGraph/llm_profile_extractor.py NeuralGraph/llm_profile_query.py NeuralGraph/tesseract.py .gitignore
2. Python: /Users/nurmanmahammadov/Desktop/NeuralGraph/.venv/bin/python. Run from your worktree's demo/ dir: `cd <worktree>/demo && <env> ../../../../.venv/bin/python -u runner.py` (or use the absolute venv path). Set EMB_CACHE_DIR is NOT used by runner (it embeds live; fine).
3. LM Studio (http://127.0.0.1:1234, google/gemma-4-e4b, nomic v1.5) is a single shared bottleneck. Before ANY run that calls the LLM:
   - wait until /private/tmp/claude-501/-Users-nurmanmahammadov-Desktop-NeuralGraph/86b8b88e-d6ea-43ee-b99d-38ce1e4a3c41/scratchpad/queue2_done exists
   - wait until /private/tmp/claude-501/-Users-nurmanmahammadov-Desktop-NeuralGraph/86b8b88e-d6ea-43ee-b99d-38ce1e4a3c41/scratchpad/lmstudio.lock does NOT exist, then create it with your hypothesis id as content; delete it when your runs are done (also on failure).
   Waiting = a foreground Bash `until` loop with sleep 30 (each Bash call may last up to 10 min; loop again if needed). Never stop your turn to "wait for a notification".
4. Comparison protocol: ONLY_CAT=single_hop MAX_QUESTIONS=100 RETRIEVAL_MODE=local_pairs, RUN_NAME=<Hx>_treatment (and, if your change has a flag, RUN_NAME=<Hx>_control with the flag off; otherwise compare against the first 100 single_hop rows (same question ids, conv order) of /Users/nurmanmahammadov/Desktop/NeuralGraph/demo/results/local_pairs_single_hop.json). Report: accuracy on the same ids, mean e2e latency, "gold substring present in final context" rate, and 3 example wins/losses. A 100-question run takes ~15 min.
5. Report file: /private/tmp/claude-501/-Users-nurmanmahammadov-Desktop-NeuralGraph/86b8b88e-d6ea-43ee-b99d-38ce1e4a3c41/scratchpad/overnight/<Hx>_<name>.md (under 600 words): result table, verdict WORKS/DOES NOT WORK/MIXED with delta, code changes path:line, cost. Copy your result JSONs next to it. Do not commit. Do not stop until the report file exists and the lock is released.
